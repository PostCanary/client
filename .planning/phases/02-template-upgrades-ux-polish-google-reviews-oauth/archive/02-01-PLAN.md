---
phase: 2
plan: 01
title: "Wave 0 — Asset Acquisition + Demo Seeds + Render Script"
wave: 0
depends_on: []
files_modified:
  - src/assets/trust-badges/ (NEW directory)
  - src/assets/trust-badges/bbb.svg (NEW)
  - src/assets/trust-badges/angi.svg (NEW)
  - src/assets/trust-badges/homeadvisor.svg (NEW)
  - src/assets/trust-badges/README.md (NEW — legal rationale)
  - src/pages/dev/PostcardPreview.vue
  - scripts/render-postcard-preview.mjs (NEW)
  - package.json
  - demo-smoke-test-2026-04-10/ (NEW directory for the rebuilt render output — actual dated folder created by executor)
requirements: [TMPL-07]
autonomous: false
estimated_hours: 2
---

## Objective

Stage every non-code asset and tooling hook required before any visual rebuild work begins: real trust-badge artwork, the Desert Diamond logo + hero photo seeds, and a one-shot Playwright render command that will be the feedback loop for every subsequent wave.

## Context

Wave 0 is the "references open, code closed" phase of the IP firewall (D-15 to D-19). Nothing here touches the postcard rendering — it stages data and tooling that Wave 2 consumes when references are CLOSED. Per RESEARCH.md RISK-05 (trust badge acquisition), RISK-02 (Firecrawl logo gap), and RISK-06 (photo seed required before any visual checkpoint can look right), these must land before Wave 1 starts.

Key decisions grounded in CONTEXT.md:
- D-10: Manually seed desertdiamondhvac.com's real logo into the dev preview (production fallback is the wordmark, delivered in Plan 02).
- D-11: Replace the loyalty-graphic hero with a worker/technician photo from the 13 already-scraped photos.
- D-13 + D-14: Use real BBB / Angi / HomeAdvisor brand artwork under nominative fair use; document rationale.
- RESEARCH.md §Validation Architecture: "Playwright script for `/dev/postcard-preview`" is the manual visual feedback loop — no Vitest in scope.

This plan is `autonomous: false` because trust badge artwork sourcing and the desertdiamondhvac.com logo grab require Drake-in-the-loop approval on the final assets (Drake confirms "yes these are the correct real-brand logos" before Wave 2 depends on them).

## Tasks

<task>
<task_id>02-01-01</task_id>
<title>Create the trust-badges asset directory and download real BBB / Angi / HomeAdvisor artwork</title>

<read_first>
- `~/postcanary/client/.planning/phases/02-template-upgrades-ux-polish-google-reviews-oauth/02-CONTEXT.md` (D-13, D-14 — real brand artwork requirement and nominative fair use rationale)
- `~/postcanary/client/.planning/phases/02-template-upgrades-ux-polish-google-reviews-oauth/02-RESEARCH.md` §"P-14, P-15, P-30, P-31" and §"RISK-05 Trust Badge Real Artwork Acquisition"
- `~/postcanary/client/src/assets/` (current asset directory structure for placement reference)
</read_first>

<action>
1. Create directory: `~/postcanary/client/src/assets/trust-badges/` (use `mkdir -p`).
2. Acquire three SVG or PNG brand assets and save them with these exact filenames:
   - `src/assets/trust-badges/bbb.svg` — BBB Accredited Business seal, navy blue (hex `#003087`), "A+" rating variant if available. Official source: https://www.bbb.org/brand-assets (or equivalent official BBB brand portal). If SVG is unavailable, a ≥300 DPI transparent-background PNG is acceptable as `bbb.png`.
   - `src/assets/trust-badges/angi.svg` — Angi (formerly Angie's List) Certified or Super Service Award logo. Official source: https://www.angi.com/brand / press kit page. Brand color: Angi orange `#F26B2C`. PNG fallback: `angi.png`.
   - `src/assets/trust-badges/homeadvisor.svg` — HomeAdvisor "Top Rated" or "Screened & Approved" badge. HomeAdvisor was rolled into Angi, but the HomeAdvisor shield/green artwork is still issued. Brand color: HomeAdvisor green/orange `#F7931E`. PNG fallback: `homeadvisor.png`.
3. **Drake is in the loop for this step.** Before running any of these downloads, the executor MUST ask Drake: "Drake — I need the official BBB, Angi, and HomeAdvisor brand assets. Do you (a) want me to download them from each org's public brand/press kit pages, (b) have existing asset files you want me to drop in, or (c) accept styled-div facsimiles in the correct brand colors (BBB navy, Angi orange, HomeAdvisor green) with a TODO comment marking the swap point?" Wait for an explicit pick before proceeding. Do NOT use WebFetch (banned). If option (a) is chosen, Drake runs the downloads in his own browser and drops files into `src/assets/trust-badges/`.
4. If option (c) is chosen, the executor creates three 1x1-pixel placeholder SVGs (`bbb.svg`, `angi.svg`, `homeadvisor.svg`) filled with the correct hex color and with a `<!-- TODO: swap with real BBB brand artwork, nominative fair use per D-14 -->` XML comment at the top, so Plan 03's `TrustBadges.vue` rewrite has a valid `import` target and the real swap is a one-file change later.
5. Verify each file exists at its expected path before proceeding to the next task.
</action>

<acceptance_criteria>
- `ls ~/postcanary/client/src/assets/trust-badges/` lists at minimum `bbb.svg` (or `bbb.png`), `angi.svg` (or `angi.png`), `homeadvisor.svg` (or `homeadvisor.png`).
- Each file is >0 bytes: `find ~/postcanary/client/src/assets/trust-badges/ -type f -size +0c | wc -l` ≥ 3.
- If placeholder SVGs (option c) were used: `grep -l "TODO.*nominative fair use" ~/postcanary/client/src/assets/trust-badges/*.svg` returns all three files.
- Drake has explicitly confirmed which of (a)/(b)/(c) was chosen; do not mark task done without that confirmation in the session transcript.
</acceptance_criteria>

<rollback_plan>
Delete `~/postcanary/client/src/assets/trust-badges/` entirely. No other code depends on this directory yet at Wave 0 — Wave 2 Plan 03's TrustBadges rewrite is the first consumer.
</rollback_plan>
</task>

<task>
<task_id>02-01-02</task_id>
<title>Add legal rationale README for trust badge assets</title>

<read_first>
- `~/postcanary/client/.planning/phases/02-template-upgrades-ux-polish-google-reviews-oauth/02-CONTEXT.md` §"D-14" (nominative fair use rationale)
- `~/postcanary/client/src/assets/trust-badges/` (from previous task)
</read_first>

<action>
Create `~/postcanary/client/src/assets/trust-badges/README.md` with this EXACT content:

```markdown
# Trust Badge Artwork — Legal Rationale

## Purpose
These assets (`bbb.svg`, `angi.svg`, `homeadvisor.svg`) render the real brand marks of third-party certifications that customer businesses hold. They are used on customer-facing postcard templates where the customer's business is ACTUALLY BBB-accredited, Angi-certified, or HomeAdvisor-rated.

## Legal Basis: Nominative Fair Use
Per CONTEXT.md D-14 and the three-factor nominative fair use test (New Kids on the Block v. News America Publishing):

1. **The product/service cannot be readily identified without using the mark.** BBB accreditation, Angi certification, and HomeAdvisor ratings cannot be identified without using the actual organization name and logo — these marks ARE the identifier.
2. **Only so much of the mark is used as is reasonably necessary to identify the product/service.** The badges are rendered at their natural brand proportions and colors. No modification, stretching, or creative reinterpretation.
3. **The user does nothing that would, in conjunction with the mark, suggest sponsorship or endorsement.** PostCanary does not claim BBB/Angi/HomeAdvisor endorse PostCanary. The badges appear on postcards for businesses that have earned those credentials.

## Scope of Use
- Allowed: Customer businesses that genuinely hold the credential (verified via their BrandKit.trustBadges array during scraping).
- Not allowed: Customer businesses that do NOT hold the credential. The `TrustBadges.vue` component MUST route each badge by `type` and only render the real artwork when the customer's scraped `trustBadges[]` includes a matching entry.

## Source Attribution
Each SVG/PNG in this directory was sourced from the respective organization's official brand assets portal. No artwork was modified or recolored away from the published brand specifications.

## Swap Path for Production
If legal counsel later requires a different approach (license agreement, text-facsimile fallback), the swap point is `src/components/design/TrustBadges.vue` and this directory. Every consumer goes through that one component.
```
</action>

<acceptance_criteria>
- `test -f ~/postcanary/client/src/assets/trust-badges/README.md` exits 0.
- `grep -c "Nominative Fair Use" ~/postcanary/client/src/assets/trust-badges/README.md` ≥ 1.
- `grep -c "New Kids on the Block" ~/postcanary/client/src/assets/trust-badges/README.md` ≥ 1.
</acceptance_criteria>
</task>

<task>
<task_id>02-01-03</task_id>
<title>Seed Desert Diamond real logo into the dev preview route</title>

<read_first>
- `~/postcanary/client/.planning/phases/02-template-upgrades-ux-polish-google-reviews-oauth/02-CONTEXT.md` §"D-10" (manual logo seed)
- `~/postcanary/client/.planning/phases/02-template-upgrades-ux-polish-google-reviews-oauth/02-RESEARCH.md` §"RISK-02" (Firecrawl CSS background-image gap)
- `~/postcanary/client/src/pages/dev/PostcardPreview.vue` — the dev route the executor is modifying
</read_first>

<action>
This task seeds the logo into the DEV PREVIEW ROUTE only, not into a real database. The production dev DB update is a separate concern, but the Playwright render (used for every visual checkpoint in Wave 2/3) reads from `src/pages/dev/PostcardPreview.vue`, so THAT is the file that must carry the Desert Diamond logo seed for the demo render.

1. **Acquire the Desert Diamond logo.** Ask Drake: "For the Desert Diamond logo seed, I can (a) ask you to drop a logo file at `~/postcanary/client/src/assets/desert-diamond-logo.png` that you've downloaded from desertdiamondhvac.com yourself, or (b) generate a temporary wordmark placeholder that uses the brand orange as a fill with 'DESERT DIAMOND HVAC' text, marked TODO for swap. Which one?" Do NOT use WebFetch or browser automation to scrape desertdiamondhvac.com — Drake does the asset acquisition step.
2. Save the file as `~/postcanary/client/src/assets/desert-diamond-logo.png` (or `.svg` if that's what Drake provides). The path is the seed location.
3. Update `src/pages/dev/PostcardPreview.vue`:
   - Add an import at the top of `<script setup>`:
     ```ts
     import desertDiamondLogo from "@/assets/desert-diamond-logo.png";
     ```
     (Use the correct extension based on what Drake provided; if SVG, also update the import to use `?url` if vite-svg-loader conflicts: `import desertDiamondLogo from "@/assets/desert-diamond-logo.svg?url";`)
   - Add a new reactive ref (place it near the existing `brandPrimary` ref around line 127):
     ```ts
     const logoUrl = ref<string | null>(desertDiamondLogo);
     ```
   - Find the `<PostcardFront ... />` and `<PostcardBack ... />` invocations in the template. Pass `:logo-url="logoUrl"` as a new prop to `<PostcardFront>` (PostcardBack does not take logoUrl today — do not add it there).
4. Change the default form values to Desert Diamond HVAC (these were "Martinez Plumbing" from earlier sessions, per RESEARCH.md §13 — the dev route is the demo canvas):
   - `businessName.value` default → `"Desert Diamond HVAC"`
   - `city.value` default → `"Phoenix"`
   - `state.value` default → `"AZ"`
   - `streetAddress.value` default → whatever the real Desert Diamond street address is (ask Drake: "what street address should I seed for Desert Diamond?"). Do not invent one.
   - `zip.value` default → real Desert Diamond ZIP (ask Drake).
   - `phone.value` default → `"(623) 246-2377"` (confirmed in CONTEXT.md §specifics)
   - `website.value` default → `"desertdiamondhvac.com"`
   - `brandPrimary.value` default → `"#0488F5"` (Desert Diamond blue, confirmed in CONTEXT.md §specifics)
   - `brandDark.value` default → `"#F97B22"` (Desert Diamond orange)
   - `headline.value` default → `"Phoenix Homeowners: Your AC Tune-Up Is Due"` (keep — Caples formula, location-specific)
   - `rating.value` default → `4.9` (keep — real Desert Diamond rating)
   - `reviewCount.value` default → `2423` (CONTEXT.md §specifics confirmed real count)
5. Run `cd ~/postcanary/client && npx vue-tsc --noEmit` to confirm the new import and prop pass through without type errors.
</action>

<acceptance_criteria>
- `ls ~/postcanary/client/src/assets/desert-diamond-logo.*` returns at least one file of >0 bytes.
- `grep -c 'desert-diamond-logo' ~/postcanary/client/src/pages/dev/PostcardPreview.vue` ≥ 1.
- `grep -c 'Desert Diamond HVAC' ~/postcanary/client/src/pages/dev/PostcardPreview.vue` ≥ 1.
- `grep -c 'logo-url' ~/postcanary/client/src/pages/dev/PostcardPreview.vue` ≥ 1 (the prop pass).
- `grep -c 'businessName = ref("Martinez Plumbing")' ~/postcanary/client/src/pages/dev/PostcardPreview.vue` equals 0 (the old default is gone).
- `cd ~/postcanary/client && npx vue-tsc --noEmit` exits 0.
</acceptance_criteria>

<rollback_plan>
Revert the file changes with git. Remove the new asset file. No other code depends on these changes yet.
</rollback_plan>
</task>

<task>
<task_id>02-01-04</task_id>
<title>Seed Desert Diamond hero photo (worker/technician image) into the dev preview route</title>

<read_first>
- `~/postcanary/client/.planning/phases/02-template-upgrades-ux-polish-google-reviews-oauth/02-CONTEXT.md` §"D-11, D-12" (hero photo priority: people > work > equipment > abstract)
- `~/postcanary/client/.planning/phases/02-template-upgrades-ux-polish-google-reviews-oauth/02-RESEARCH.md` §"RISK-06" (photo seed required BEFORE visual checkpoints)
- `~/postcanary/client/src/pages/dev/PostcardPreview.vue` (the `photoUrl` ref around line 123)
</read_first>

<action>
1. Ask Drake: "Of the 13 already-scraped Desert Diamond photos, which is the demo hero? Per D-11 / P-32 I need a worker, technician, or homeowner image — not the loyalty graphic currently used. Can you (a) give me the URL or filename of the one you want, or (b) confirm that I should pick the best-scoring worker photo from `~/postcanary/client/src/assets/desert-diamond-hero.jpg` if you drop one there, or (c) use a placeholder Unsplash URL of an HVAC technician while you select the real one?" Wait for Drake's pick.
2. If option (a) — drop Drake's chosen URL into `photoUrl.value` default in `src/pages/dev/PostcardPreview.vue` (around line 123). If it's a file path, save as `~/postcanary/client/src/assets/desert-diamond-hero.jpg` and import similar to the logo.
3. If option (b) — Drake drops the file, executor imports it and sets `photoUrl.value` to the imported URL.
4. If option (c) — use `https://images.unsplash.com/photo-1581094794329-c8112a89af12?w=1200` (HVAC technician hero photo from Unsplash, commercial-use friendly) as the TEMPORARY seed with an inline comment: `// TODO D-11: replace with real scraped Desert Diamond worker photo before demo render`.
5. **The previous photoUrl default points to `unsplash.com/photo-1504917595217-d4dc5ebe6122`** (a generic residential street scene per the existing code — see line 124). This must be replaced. The replacement photo must show a PERSON (worker, technician, or homeowner) per P-32 — equipment-only and abstract-graphic shots fail the pattern check.
6. Run `cd ~/postcanary/client && npx vue-tsc --noEmit`.
</action>

<acceptance_criteria>
- `grep -c '1504917595217-d4dc5ebe6122' ~/postcanary/client/src/pages/dev/PostcardPreview.vue` equals 0 (the old generic photo is gone).
- `grep -c 'photoUrl = ref' ~/postcanary/client/src/pages/dev/PostcardPreview.vue` ≥ 1 (the ref still exists with a new value).
- If option (c) was used: `grep -c 'TODO D-11' ~/postcanary/client/src/pages/dev/PostcardPreview.vue` ≥ 1.
- Drake has confirmed the photo choice in-session.
- `cd ~/postcanary/client && npx vue-tsc --noEmit` exits 0.
</acceptance_criteria>
</task>

<task>
<task_id>02-01-05</task_id>
<title>Wire the one-shot Playwright render script for the dev preview</title>

<read_first>
- `~/postcanary/client/.planning/phases/02-template-upgrades-ux-polish-google-reviews-oauth/02-RESEARCH.md` §"Validation Architecture" and §"RISK-03" (no client test runner; visual checkpoints are manual + Playwright)
- `~/postcanary/client/.planning/phases/02-template-upgrades-ux-polish-google-reviews-oauth/02-VALIDATION.md` §"Wave 0 Requirements" item 4 (Playwright command)
- `~/postcanary/client/package.json` (scripts block; current dev command is `vite`)
- `~/postcanary/client/scripts/` (existing node scripts — example structure for `.mjs` tooling scripts)
- `~/postcanary/client/src/pages/dev/PostcardPreview.vue` (route at `/dev/postcard-preview` per `src/router.ts:215`)
</read_first>

<action>
1. Install `playwright` as a dev dependency IF it is not already available at the repo level. Check: `cd ~/postcanary/client && node -e "try { require.resolve('playwright'); console.log('present'); } catch { console.log('missing'); }"`. If `missing`, run `cd ~/postcanary/client && npm install --save-dev playwright && npx playwright install chromium` (chromium only, not the full browser set — keep it light).
2. Create `~/postcanary/client/scripts/render-postcard-preview.mjs` with this content:

```javascript
// scripts/render-postcard-preview.mjs
//
// Renders the /dev/postcard-preview route for the Desert Diamond HVAC demo
// and writes PNG screenshots of the front and back to demo-smoke-test-<DATE>/.
//
// Assumes `npm run dev` is ALREADY running on http://localhost:5173 (or
// whatever port Vite has picked — pass via DEV_URL env var if non-default).
//
// Usage:
//   npm run dev               # in one terminal
//   npm run render-postcard-preview   # in another terminal
//
// Writes:
//   demo-smoke-test-<YYYY-MM-DD>/desert-diamond-FRONT.png
//   demo-smoke-test-<YYYY-MM-DD>/desert-diamond-BACK.png

import { chromium } from "playwright";
import { mkdirSync, existsSync } from "fs";
import { resolve, dirname } from "path";
import { fileURLToPath } from "url";

const __dirname = dirname(fileURLToPath(import.meta.url));
const repoRoot = resolve(__dirname, "..");
const devUrl = process.env.DEV_URL ?? "http://localhost:5173";
const route = "/dev/postcard-preview";

const today = new Date().toISOString().slice(0, 10); // YYYY-MM-DD
const outDir = resolve(repoRoot, `demo-smoke-test-${today}`);
if (!existsSync(outDir)) {
  mkdirSync(outDir, { recursive: true });
}

const browser = await chromium.launch();
const context = await browser.newContext({
  viewport: { width: 1440, height: 900 },
  deviceScaleFactor: 2, // Retina — crisper PNGs for visual inspection
});
const page = await context.newPage();

console.log(`Navigating to ${devUrl}${route}`);
await page.goto(`${devUrl}${route}`, { waitUntil: "networkidle" });

// Wait for the front card to be visible. The dev route shows both front
// and back stacked vertically. Screenshot each by selector.
await page.waitForSelector(".pc-card", { timeout: 10000 });

const cards = await page.locator(".pc-card").all();
if (cards.length < 2) {
  throw new Error(`Expected 2 .pc-card elements (front + back), found ${cards.length}`);
}

const frontPath = resolve(outDir, "desert-diamond-FRONT.png");
const backPath = resolve(outDir, "desert-diamond-BACK.png");

await cards[0].screenshot({ path: frontPath });
console.log(`Wrote ${frontPath}`);

await cards[1].screenshot({ path: backPath });
console.log(`Wrote ${backPath}`);

await browser.close();
console.log("Done.");
```

3. Add an `npm run` script for it by editing `~/postcanary/client/package.json`. In the `"scripts"` object, after the `"preview"` line, add:
```
    "render-postcard-preview": "node scripts/render-postcard-preview.mjs"
```
Ensure the JSON remains valid (trailing comma rules) — the new line goes after the existing `"preview": "vite preview"` entry with a comma appended to the previous line.

4. **Smoke-test the script.** In one terminal: `cd ~/postcanary/client && npm run dev`. Wait for Vite to report the port. If the port is not 5173, note the actual port (e.g. 5175). In a second terminal: `cd ~/postcanary/client && DEV_URL=http://localhost:<actual-port> npm run render-postcard-preview`. Confirm the script completes without error and PNGs appear in `demo-smoke-test-<today's-date>/`.

5. If the script fails because there are stacked cards but `.pc-card` matches multiple elements in an unexpected order, adjust the `cards[0]` / `cards[1]` indices. Front is rendered before back in the current PostcardPreview.vue structure, so cards[0] = front, cards[1] = back. Verify this by opening one of the PNGs and confirming which side it is.
</action>

<acceptance_criteria>
- `test -f ~/postcanary/client/scripts/render-postcard-preview.mjs` exits 0.
- `grep -c 'render-postcard-preview' ~/postcanary/client/package.json` ≥ 1.
- `grep -c 'node scripts/render-postcard-preview.mjs' ~/postcanary/client/package.json` ≥ 1.
- `cd ~/postcanary/client && node -e "try { require.resolve('playwright'); console.log('ok'); } catch { process.exit(1); }"` exits 0.
- After a smoke-test run, `ls ~/postcanary/client/demo-smoke-test-*/desert-diamond-FRONT.png` returns at least one file.
- After a smoke-test run, `ls ~/postcanary/client/demo-smoke-test-*/desert-diamond-BACK.png` returns at least one file.
- Both PNG files are >5000 bytes (non-empty renders): `find ~/postcanary/client/demo-smoke-test-*/desert-diamond-*.png -size +5000c | wc -l` ≥ 2.
- `cd ~/postcanary/client && npx vue-tsc --noEmit` exits 0 (confirm the dev route changes from 02-01-03 / 02-01-04 still pass type check).
</acceptance_criteria>

<rollback_plan>
- Delete `scripts/render-postcard-preview.mjs`.
- Remove the `render-postcard-preview` line from `package.json` scripts.
- Optionally `npm uninstall playwright` if it was newly added. Vite dev server is unaffected.
</rollback_plan>
</task>

## Verification

Run, in order:
1. `ls ~/postcanary/client/src/assets/trust-badges/` — should list BBB/Angi/HomeAdvisor files + README.md.
2. `cd ~/postcanary/client && npx vue-tsc --noEmit` — exits 0.
3. Boot dev server + run `npm run render-postcard-preview` in a second terminal — two PNGs land in `demo-smoke-test-<today>/`.
4. Open `demo-smoke-test-<today>/desert-diamond-FRONT.png` in Explorer. Visually confirm:
   - Real Desert Diamond logo appears top-left (not a text span fallback).
   - Hero photo shows a person (worker/technician/homeowner), not a loyalty graphic.
   - Headline says "Phoenix Homeowners: ..." style.
   - Phone number is `(623) 246-2377`.
5. Note visible Draplin violations you expect Wave 1+2 to fix (rounded corners, soft shadows, outlined trust badges) — these are NOT fixed by Wave 0.

## Must-Haves

- Real BBB / Angi / HomeAdvisor brand artwork (or Drake-approved facsimiles with TODO markers) exist under `src/assets/trust-badges/`.
- A legal rationale README exists alongside the badge assets.
- The dev preview route `/dev/postcard-preview` renders with Desert Diamond HVAC as the default business, the real logo, the real brand colors (#0488F5 / #F97B22), and a worker-type hero photo.
- `npm run render-postcard-preview` produces two PNGs of the front and back, proving the Wave 2/3 feedback loop is functional.
- Zero TypeScript errors from `vue-tsc --noEmit` after all Wave 0 changes.

## Threat Model

**ASVS Level 1 minimal — asset staging, no new auth/data flow.**

| Threat | Severity | Mitigation |
|---|---|---|
| Trust badge artwork licensing (BBB/Angi/HomeAdvisor logos) | MEDIUM | Nominative fair use rationale documented in `src/assets/trust-badges/README.md` per D-14. Swap path is contained to `TrustBadges.vue` + this directory if legal counsel later requires a different approach. |
| Malicious content in downloaded SVG (script tags, XSS) | LOW | SVG assets are imported via Vite's asset pipeline which serves them as static files, not parsed into DOM. The `TrustBadges.vue` rewrite in Plan 03 will use `<img src="@/assets/trust-badges/bbb.svg">` — image src, not innerHTML — which is the safe path. |
| Playwright downloading arbitrary browsers during `npx playwright install` | LOW | Install command is pinned to `chromium` only. No network calls beyond the public Playwright CDN during the initial install. |
| Hardcoded external photo URL (Unsplash placeholder in option c of 02-01-04) | LOW | Only used if Drake picks option (c). Marked with TODO comment. Not production code — dev route only. |

No new authentication, no new data persistence, no new API endpoints.
