node_modules
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
dist
.vercel
playwright-report
test-results
.playwright-cli

# vue-tsc --build emit artifacts
*.tsbuildinfo
src/**/*.js

# Working / demo / audit artifacts — local output, not source
screenshots/
demo-smoke-test-*/
visual-audit-*/
.planning/phases/*/spikes/*/output/

# Playwright CLI output
test-results/
playwright-report/
blob-report/
test-results-live/
playwright-report-live/
playwright-report-release/
.auth/


# firecrawl scrape cache (research artifacts, not source)
.firecrawl/

# Raw brand kit (EPS/PDF/source PNGs) — web-ready copies live in src/assets/brand/
branding-kit/
